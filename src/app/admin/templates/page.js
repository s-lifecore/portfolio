import AdminTemplatesClient from '../../../components/AdminTemplatesClient';

export const metadata = { title: '管理 - テンプレート' };

export default function Page() {
    return (
        <div>
            <AdminTemplatesClient />
        </div>
    );
}
